{
  "name": "dmbo-sim",
  "version": "0.1.0",
  "description": "DMBO simulator and acceptance harness",
  "type": "module",
  "scripts": {
    "build:orchestrator": "cargo build --manifest-path ../orchestrator/Cargo.toml --quiet",
    "test": "npm run build:orchestrator && node --test --test-concurrency=1",
    "acceptance": "npm run build:orchestrator && node ./src/run-acceptance.js"
  },
  "license": "MIT"
}
